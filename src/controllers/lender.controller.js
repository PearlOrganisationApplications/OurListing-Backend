export const getDashboard = async (req, res) => {
  res.status(200).json({ message: "Lender Dashboard placeholder" });
};

export const getMortgages = async (req, res) => {
  res.status(200).json({ message: "Lender Mortgages placeholder" });
};
