export const getDashboard = async (req, res) => {
  res.status(200).json({ message: "Owner Dashboard placeholder" });
};

export const getListings = async (req, res) => {
  res.status(200).json({ message: "Owner Listings placeholder" });
};

export const addProperty = async (req, res) => {
  res.status(200).json({ message: "Add Property placeholder" });
};
